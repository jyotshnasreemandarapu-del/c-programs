/*let's undrstand the ternary or conditional operators through an example*/
#include<stdio.h>
int main()
{
	int age;//variable declaration 
	printf("enter your age");
	scanf("%d",&age);
	(age>=18);
	(printf("eligible for voting"));
	(printf("eligible for voting"));
	return 0;
}